apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: broken-bank
  region: eu-west-2
  version: "1.31"

managedNodeGroups:
  - name: demo-nodes
    instanceType: t3.medium
    desiredCapacity: 1
    minSize: 1
    maxSize: 2
    volumeSize: 30
